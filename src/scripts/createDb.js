/**
 * this is the command being used for creating the table name myCustomers
 * and this will create if not there and switched the user to that 
 */
use myCustomers


/**
 * Command to see which Db we are currently in
 */
db